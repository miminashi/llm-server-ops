# Pre-swap dmesg tail: skipped (journalctl -k requires sudo on this system, kernel.dmesg_restrict=1)
# Pre-swap baseline (lspci + rocm-smi) is sufficient for slot/identity verification.
# Telemetry kern_dmesg.log during the campaign will capture all kernel events during the test window.
